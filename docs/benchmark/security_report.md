# Security Test Report

Generated by `go test -run TestZZZSecurityReport ./...` (or `make report`).
Do not edit by hand — regenerate.

* Date: 2026-08-06
* Platform: darwin/arm64
* Baseline: `/usr/bin/unzip`

Each fixture is run through both tools. The test asserts the *difference*: secure-unzip defuses the payload while the standard tool does not.

| Fixture | Control | secure-unzip | system unzip | Result |
|---------|---------|--------------|--------------|--------|
| `benign` | (control case — no payload) | extracted all 5 files, exit 0 | extracted normally | PASS |
| `inodes` | -max-files | aborted at 1,000 entries, exit 3 | UNSAFE: extracted all 20000 entries | PASS |
| `perms` | -max-mode | masked 0777 to 0755 and stripped setuid | UNSAFE: preserved mode 0777 | PASS |
| `symlink` | symlink target containment | aborted, exit 3 | UNSAFE: created a symlink pointing at /etc/passwd | PASS |
| `zipbomb` | -max-size / -max-ratio | aborted at the 1 MiB ceiling, exit 3, nothing written | UNSAFE: expanded 67108864 bytes unbounded | PASS |
| `zipslip` | zip-slip path containment | aborted, exit 3, named the constraint | did not exhibit the unsafe behaviour | PASS |

6/6 fixtures passed.

## Notes

* Fixtures are generated at test time by `testutils/fixtures.go`; no malicious archive is committed to the repository.
* The system-unzip arm runs inside a sandbox directory nested deeply enough that a `../../etc/passwd` payload lands inside the sandbox, never on the real `/etc`.
* A `--secure=no` run disables these controls by design; see `docs/agents/decisions/2026-08-06-secure-master-switch.md`.
