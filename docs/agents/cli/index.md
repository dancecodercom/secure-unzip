# CLI

secure-unzip is a command-line tool meant to be a drop-in alternative to standard `unzip`. This
section records the exact surface: syntax, flags, defaults, and the compatibility contract.

* [Command Line Interface](command-line-interface.md) - syntax, every flag, defaults, open questions
